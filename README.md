# ICON-2021 Shared Task

## Install Requirements

* Set your current directory to the project directory
* Then copy and paste the following codes
    ```bash
    pip install pipenv
    ```
    ```bash
    pipenv install
    ```
